# LePetPal Frontend Environment Variables

# Backend API Base URL
# For local development, use http://localhost:5000
# For LAN deployment, use your backend server IP (e.g., http://192.168.1.100:5000)
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000

# Optional: Default video stream dimensions
# These can be overridden in the UI configuration panel
NEXT_PUBLIC_DEFAULT_VIDEO_WIDTH=1280
NEXT_PUBLIC_DEFAULT_VIDEO_HEIGHT=720

# Optional: SSE reconnection settings
# Maximum delay between reconnection attempts in milliseconds
NEXT_PUBLIC_SSE_RECONNECT_MAX_DELAY=8000
